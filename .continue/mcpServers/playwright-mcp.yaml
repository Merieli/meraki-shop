name: Playwright mcpServer
version: 0.0.1
schema: v1
mcpServers:
  - name: Browser search
    type: stdio
    command: npx
    args:
      - "-y"
      - "@playwright/mcp@latest"
    cwd: \\wsl$\Debian\home\merieli\projects\meraki-shop